# Shell scripts run in Linux Docker; CRLF breaks shebang and control flow on Alpine.
# patch-package / patches apply with patch(1); CRLF breaks application on Windows.
patches/** text eol=lf
*.sh text eol=lf
docker/start.sh text eol=lf
docker/start-all-in-one.sh text eol=lf
